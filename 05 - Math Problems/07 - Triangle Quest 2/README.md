<h1 align='center'>Triangle - Quest - 2</h1>

## Problem Statement

**Problem URL :** [Traingle Quest 2](https://www.hackerrank.com/challenges/triangle-quest-2/problem?isFullScreen=true)

![image](https://github.com/user-attachments/assets/aecc659e-eb95-4bc4-8c19-38ae8e83c337)
![image](https://github.com/user-attachments/assets/9ac40ef3-a579-4cde-ba39-34e7bbf52cb0)


## Problem Solution
```py
for i in range(1, int(input())+1): 
    print(((10**i-1)//9)**2)
```

## Problem Solution Explanation
Let's break down the code you provided to understand how it generates the pattern:

### Input Reading
```python
int(input())
```
- `input()` reads a line of input from the user.
- `int(input())` converts this input into an integer, which represents the number of rows, `n`, for the pattern.

### Loop
```python
for i in range(1, int(input()) + 1):
```
- This loop iterates from `1` to `n` (inclusive). The loop variable `i` represents the current row number.

### Pattern Calculation
```python
((10**i - 1) // 9) ** 2
```
Let's break down this calculation step-by-step:

1. **Calculate `10**i - 1`**:
   - `10**i` computes 10 raised to the power of `i`. For example, if `i = 3`, `10**i` is `1000`.
   - Subtracting `1` gives us a number consisting of `i` repetitions of `9`. For `i = 3`, `10**3 - 1` equals `999`.

2. **Divide by `9`**:
   - `((10**i - 1) // 9)` divides the result by `9`. This converts the number of `9`s into a number consisting of `i` repetitions of `1`. For `i = 3`, `999 // 9` equals `111`.

3. **Square the Result**:
   - `((10**i - 1) // 9) ** 2` squares the resulting number. For `i = 3`, `111 ** 2` equals `12321`.

### Example Walkthrough

Let’s see how this works with specific examples:

- **For `i = 1`**:
  - `10**1 - 1` gives `9`.
  - `9 // 9` gives `1`.
  - `1 ** 2` equals `1`.

- **For `i = 2`**:
  - `10**2 - 1` gives `99`.
  - `99 // 9` gives `11`.
  - `11 ** 2` equals `121`.

- **For `i = 3`**:
  - `10**3 - 1` gives `999`.
  - `999 // 9` gives `111`.
  - `111 ** 2` equals `12321`.

- **For `i = 4`**:
  - `10**4 - 1` gives `9999`.
  - `9999 // 9` gives `1111`.
  - `1111 ** 2` equals `1234321`.

### Summary

The code generates a pattern of numbers where each row `i` forms a palindrome of length `2i - 1`. The pattern is generated by calculating a number with `i` ones (like `111` for `i = 3`), and then squaring this number to get the required pattern for that row. The result is a series of palindromic numbers that grow in size with each row.
